# Archive

Everything moved out of `code/` when the project was reduced to the current baseline strategy. Nothing was deleted — safe to remove this folder entirely if you never need it.

- `scripts/` — old experiment scripts (VIX tests, permutation tests, sweeps, TP/stop studies, multi-symbol pipeline, plotting helpers) plus full pre-trim copies of `evaluate_regime_balancing_full.py` / `evaluate_compounding_regimes_full.py` and the `backup_pure_ict/` and `backup_c1_c3_approved/` strategy snapshots.
- `results_and_reports/` — raw output files from every study (see `EXPERIMENT_HISTORY.md` in the parent folder for what each one was).
- `old_outputs/` — stale plots and redundant root-level CSV copies.
- `data/` — VIX 1-minute futures data (2018–2025, Databento — costs credits to re-download) and leftover temp_labeled files.
